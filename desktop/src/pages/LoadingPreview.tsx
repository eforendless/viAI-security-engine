import { LoadingScreen } from "../components/LoadingScreen";

export default function LoadingPreview() {
  return <main className="loading-preview"><LoadingScreen title="Loading security center" detail="Preparing the local workspace." /></main>;
}