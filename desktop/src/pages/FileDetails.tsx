import { ArrowLeft, Download, FileBadge, FolderOpen, ShieldCheck } from "lucide-react";
import { motion } from "framer-motion";
import { useEffect, useState } from "react";
import { useNavigate, useParams } from "react-router-dom";
import { Button, DropdownMenu, LoadingState, Panel, RiskBadge } from "../components/ui";
import { pageMotion } from "../animations/motion";

interface RecordDetail { id: string; kind: string; occurredAt: string; detail: string; filePath?: string; fileHash?: string; riskScore?: number; trustScore?: number; recommendation?: string; matchedRules?: string[]; trustIndicators?: string[]; engineVersion?: string; scanDurationMs?: number; scanType?: string; report?: Record<string, unknown>; }
type ExportFormat = "excel" | "pdf" | "html" | "json";
const exportOptions = [{ value: "excel", label: "Excel" }, { value: "pdf", label: "PDF" }, { value: "html", label: "HTML" }, { value: "json", label: "JSON" }] as const;

export default function FileDetails() {
  const { id } = useParams(); const navigate = useNavigate(); const [record, setRecord] = useState<RecordDetail>(); const [loaded, setLoaded] = useState(false); const [engineVersion, setEngineVersion] = useState<string>();
  useEffect(() => { let active = true; void (async () => { try { const next = id ? await window.viai?.background.historyRecord(id) : undefined; if (active) setRecord(next as RecordDetail | undefined); } finally { if (active) setLoaded(true); } })(); void window.viai?.application.engineVersion().then((value) => { if (active) setEngineVersion(value); }); return () => { active = false; }; }, [id]);
  if (!loaded) return <motion.div {...pageMotion} className="empty-report"><LoadingState title="Loading local report" detail="Retrieving the selected analysis report." /></motion.div>;
  if (!record) return <motion.div {...pageMotion} className="empty-report"><FileBadge size={42} /><h2>Report not found</h2><p>This local report is no longer available.</p><Button onClick={() => navigate("/history")}><ArrowLeft size={16} />Back to history</Button></motion.div>;
  const report = record.report ?? {}; const metadata = object(report.metadata); const hashes = object(report.hashes); const professional = object(report.report); const reportTrust = object(professional.trust); const reportRisk = object(professional.risk); const reportConfidence = object(professional.confidence); const signature = object(report.digitalSignature); const evidence = strings(report.evidence); const displayedEngineVersion = engineVersion ?? record.engineVersion ?? "Not recorded";
  const exportReport = (format: ExportFormat) => { const exportData = buildExportReport(record, report, professional, signature, evidence, displayedEngineVersion); if (format === "html") download(htmlDocument(exportData), `${exportData.fileName}.html`, "text/html"); if (format === "json") download(JSON.stringify(jsonDocument(exportData), null, 2), `${exportData.fileName}.json`, "application/json"); if (format === "excel") download(excelDocument(exportData), `${exportData.fileName}.xls`, "application/vnd.ms-excel"); if (format === "pdf") download(pdfDocument(exportData), `${exportData.fileName}.pdf`, "application/pdf"); };
  const rows: Array<[string, string]> = [["File name", record.filePath?.split(/[\\/]/).pop() ?? "Not available"], ["Full path", record.filePath ?? "Not retained by this legacy record"], ["SHA-256", string(hashes.sha256) ?? record.fileHash ?? "Not available"], ["Timestamp", formatDate(record.occurredAt)], ["Engine version", displayedEngineVersion], ["Risk score", String(record.riskScore ?? report.finalRiskScore ?? "Not available")], ["Trust score", String(record.trustScore ?? report.trustScore ?? "Not available")], ["Recommendation", record.recommendation ?? string(report.recommendation) ?? "Not available"], ["Scan duration", record.scanDurationMs === undefined ? "Not recorded" : formatDuration(record.scanDurationMs)], ["Scan type", record.scanType ?? "Not recorded"], ["File size", typeof metadata.size === "number" ? formatBytes(metadata.size) : "Not recorded"], ["File type", string(report.fileType) ?? "Not recorded"]];
  return <motion.div {...pageMotion} className="page-stack"><div className="page-title split-title"><div><Button onClick={() => navigate("/history")} className="secondary"><ArrowLeft size={16} />Back to history</Button><p className="eyebrow">LOCAL EVIDENCE REPORT</p><h2>{record.filePath?.split(/[\\/]/).pop() ?? record.detail}</h2><p>{record.filePath ?? "Legacy local record"}</p></div><div className="details-actions">{record.filePath && <Button onClick={() => void window.viai?.openPath(record.filePath)}><FolderOpen size={16} />Open file location</Button>}<DropdownMenu ariaLabel="Export report as" label="Export as" options={exportOptions} onChange={exportReport} icon={Download} /><Button onClick={() => navigator.clipboard.writeText(JSON.stringify(record, null, 2))}>Copy report</Button></div></div>{string(professional.summary) && <Panel className="evidence-panel"><div className="panel-heading"><h3>Executive summary</h3></div><p>{string(professional.summary)}</p></Panel>}<section className="report-summary"><Panel><span className="report-icon"><ShieldCheck size={25} /></span><div><span>Risk score</span><strong>{number(reportRisk.score, record.riskScore ?? 0)}<small>/100</small></strong><RiskBadge risk={risk(number(reportRisk.score, record.riskScore ?? 0))} /></div></Panel><Panel><FileBadge size={22} /><div><span>Trust score</span><strong>{number(reportTrust.score, record.trustScore ?? 0)}<small>/100</small></strong><small>{string(reportTrust.level) ?? "Not recorded"}</small></div></Panel><Panel><FileBadge size={22} /><div><span>Confidence</span><strong>{number(reportConfidence.score, number(report.confidence, 0))}<small>/100</small></strong></div></Panel><Panel><FileBadge size={22} /><div><span>Recommendation</span><strong className="recommendation">{record.recommendation ?? "Local event"}</strong></div></Panel></section><div className="details-grid"><Panel className="detail-panel"><div className="panel-heading"><FileBadge size={18} /><h3>Report details</h3></div>{rows.map(([label, value]) => <div className="detail-row" key={label}><span>{label}</span><code>{value}</code></div>)}</Panel><Panel className="detail-panel"><div className="panel-heading"><FileBadge size={18} /><h3>Digital signature</h3></div>{[["Status", string(signature.status)], ["Publisher", string(signature.publisher)], ["Certificate issuer", string(signature.certificateIssuer)], ["Expires", string(signature.certificateExpiresAt)]].map(([label, value]) => <div className="detail-row" key={label}><span>{label}</span><code>{value ?? "Not recorded"}</code></div>)}</Panel><ListPanel title="Risk breakdown" values={arrayObjects(reportRisk.breakdown).map((item) => `${number(item.score, 0) >= 0 ? "+" : ""}${number(item.score, 0)} ${string(item.reason) ?? "Static rule"}`)} empty="No scored rule matches were retained." /><ListPanel title="Confidence explanation" values={strings(reportConfidence.explanation)} empty="No confidence explanation was retained." /><ListPanel title="Matched rules" values={record.matchedRules ?? []} empty="No matched rules were retained." /><ListPanel title="Trust indicators" values={record.trustIndicators ?? []} empty="No trust indicators were retained." /><ListPanel title="Evidence" values={evidence} empty={record.detail} /></div></motion.div>;
}

function ListPanel({ title, values, empty }: { title: string; values: string[]; empty: string }) { return <Panel className="evidence-panel"><div className="panel-heading"><div><h3>{title}</h3></div></div><ul>{(values.length ? values : [empty]).map((value, index) => <li key={`${value}-${index}`}>{value}</li>)}</ul></Panel>; }
function object(value: unknown): Record<string, unknown> { return value && typeof value === "object" ? value as Record<string, unknown> : {}; }
function strings(value: unknown): string[] { return Array.isArray(value) ? value.filter((entry): entry is string => typeof entry === "string") : []; }
function string(value: unknown): string | undefined { return typeof value === "string" ? value : undefined; }
function number(value: unknown, fallback: number): number { return typeof value === "number" && Number.isFinite(value) ? value : fallback; }
function arrayObjects(value: unknown): Record<string, unknown>[] { return Array.isArray(value) ? value.filter((entry): entry is Record<string, unknown> => Boolean(entry && typeof entry === "object")) : []; }
interface ExportReport { fileName: string; filePath: string; generatedAt: string; summary: string; verdict: string[]; riskScore: number; riskLevel: string; trustScore: number; trustLevel: string; confidence: number; priority: string; recommendation: string; recommendationReason: string; details: Array<[string, string]>; evidence: string[]; riskFactors: string[]; signature: Array<[string, string]>; }
function buildExportReport(record: RecordDetail, report: Record<string, unknown>, professional: Record<string, unknown>, signature: Record<string, unknown>, retainedEvidence: string[], engineVersion: string): ExportReport {
  const reportRisk = object(professional.risk); const reportTrust = object(professional.trust); const reportConfidence = object(professional.confidence); const riskScore = number(reportRisk.score, number(record.riskScore ?? report.finalRiskScore, 0)); const trustScore = number(reportTrust.score, number(record.trustScore ?? report.trustScore, 0)); const confidence = number(reportConfidence.score, number(report.confidence, 0)); const riskLevel = string(reportRisk.level) ?? risk(riskScore); const trustLevel = string(reportTrust.level) ?? (trustScore >= 75 ? "high" : trustScore >= 45 ? "established" : trustScore > 0 ? "limited" : "low"); const fileName = record.filePath?.split(/[\\/]/).pop() ?? "viAI analysis report"; const recommendation = record.recommendation ?? string(report.recommendation) ?? "Review the file before execution."; const details: Array<[string, string]> = [["File name", fileName], ["Full path", record.filePath ?? "Not retained by this local record"], ["Analysis timestamp", formatDate(record.occurredAt)], ["Scan type", record.scanType ?? "Static analysis"], ["Scan duration", record.scanDurationMs === undefined ? "Not recorded" : formatDuration(record.scanDurationMs)], ["Engine version", engineVersion], ["SHA-256", string(object(report.hashes).sha256) ?? record.fileHash ?? "Not available"], ["File type", string(report.fileType) ?? "Not recorded"]];
  const riskFactors = arrayObjects(reportRisk.breakdown).map((item) => `${number(item.score, 0) >= 0 ? "+" : ""}${number(item.score, 0)} ${string(item.reason) ?? "Static rule match"}`); const evidence = retainedEvidence.length ? retainedEvidence : ["Static evidence collection completed.", ...strings(reportConfidence.explanation), ...(string(professional.summary) ? ["Executive summary generated."] : [])]; const executable = /exe|dll|portable executable|script|installer/i.test(string(report.fileType) ?? fileName); const verdict = riskScore > 60 ? ["Static analysis identified elevated risk indicators.", "Do not execute this file outside an isolated investigation environment."] : ["Static analysis complete.", "No definitive indicators of malware were identified.", ...(executable ? ["The file contains executable content and its trust could not be fully established."] : []), "Dynamic sandbox analysis is recommended before execution."];
  return { fileName, filePath: record.filePath ?? "Not retained by this local record", generatedAt: formatDate(new Date().toISOString()), summary: string(professional.summary) ?? `${fileName} received a ${riskLevel} static-analysis risk assessment.`, verdict, riskScore, riskLevel, trustScore, trustLevel, confidence, priority: riskScore > 60 ? "High" : riskScore > 25 ? "Medium" : "Routine", recommendation, recommendationReason: "Static analysis collects local file evidence but cannot determine intent or behavior at runtime.", details, evidence, riskFactors, signature: [["Status", string(signature.status) ?? "Not recorded"], ["Publisher", string(signature.publisher) ?? "Not available"], ["Certificate issuer", string(signature.certificateIssuer) ?? "Not available"], ["Certificate expiry", string(signature.certificateExpiresAt) ?? "Not available"], ["Timestamped", signature.timestamped === true ? "Yes" : "No"]] };
}
function download(content: BlobPart, filename: string, type: string): void { const url = URL.createObjectURL(new Blob([content], { type })); const link = document.createElement("a"); link.href = url; link.download = filename; link.click(); URL.revokeObjectURL(url); }
function jsonDocument(data: ExportReport): object { return { schemaVersion: "1.0", reportType: "viAI static analysis report", generatedAt: data.generatedAt, file: Object.fromEntries(data.details), verdict: { statements: data.verdict, recommendation: data.recommendation, reason: data.recommendationReason }, assessment: { risk: { score: data.riskScore, outOf: 100, level: data.riskLevel, priority: data.priority }, trust: { score: data.trustScore, outOf: 100, level: data.trustLevel }, confidence: { score: data.confidence, outOf: 100 } }, evidence: data.evidence, riskFactors: data.riskFactors, digitalSignature: Object.fromEntries(data.signature), summary: data.summary }; }
function htmlDocument(data: ExportReport): string { const rows = (values: Array<[string, string]>) => values.map(([label, value]) => `<tr><th>${escapeHtml(label)}</th><td>${escapeHtml(value)}</td></tr>`).join(""); const list = (values: string[], empty: string) => `<ul>${(values.length ? values : [empty]).map((value) => `<li>${escapeHtml(value)}</li>`).join("")}</ul>`; return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>viAI report - ${escapeHtml(data.fileName)}</title><style>${reportStyles}</style></head><body><main><header><div class="brand"><span class="mark">V</span><span><strong>viAI</strong><small>SECURITY</small></span></div><div class="meta"><span>LOCAL EVIDENCE REPORT</span><time>${escapeHtml(data.generatedAt)}</time></div></header><section class="hero"><p>STATIC ANALYSIS REPORT</p><h1>${escapeHtml(data.fileName)}</h1><span>${escapeHtml(data.filePath)}</span></section><section class="verdict"><p class="label">VERDICT</p><h2>Static Analysis Complete</h2>${data.verdict.map((line) => `<p>${escapeHtml(line)}</p>`).join("")}</section><section class="metrics"><article><span>Risk score</span><strong>${data.riskScore}<small>/100</small></strong><b class="${risk(data.riskScore)}">${escapeHtml(capitalize(data.riskLevel))}</b></article><article><span>Trust score</span><strong>${data.trustScore}<small>/100</small></strong><b>${escapeHtml(capitalize(data.trustLevel))}</b></article><article><span>Confidence</span><strong>${data.confidence}<small>%</small></strong><b>${escapeHtml(data.priority)} priority</b></article></section><section><p class="label">EXECUTIVE SUMMARY</p><p class="summary">${escapeHtml(data.summary)}</p></section><div class="grid"><section><p class="label">EVIDENCE COLLECTED</p>${list(data.evidence, "No detailed evidence was retained.")}</section><section><p class="label">RISK FACTORS</p>${list(data.riskFactors, "No scored risk factors were retained.")}</section></div><section><p class="label">RECOMMENDATION</p><h2>${escapeHtml(data.recommendation)}</h2><p>${escapeHtml(data.recommendationReason)}</p></section><div class="grid"><section><p class="label">FILE DETAILS</p><table>${rows(data.details)}</table></section><section><p class="label">DIGITAL SIGNATURE</p><table>${rows(data.signature)}</table></section></div><footer><strong>viAI security</strong><span>Generated locally. Static analysis provides evidence for investigation and does not determine intent.</span></footer></main></body></html>`; }
function excelDocument(data: ExportReport): string { const row = (values: string[], style = "Cell") => `<Row>${values.map((value) => `<Cell ss:StyleID="${style}"><Data ss:Type="String">${escapeXml(value)}</Data></Cell>`).join("")}</Row>`; const sheet = (name: string, title: string, values: Array<[string, string]>) => `<Worksheet ss:Name="${escapeXml(name)}"><Table><Column ss:Width="190"/><Column ss:Width="470"/>${row(["viAI SECURITY", "LOCAL EVIDENCE REPORT"], "Brand")} ${row([title, data.fileName], "Title")} ${row(["Generated", data.generatedAt], "Subtle")} ${row(["", ""], "Cell")} ${row(["Field", "Assessment"], "Heading")}${values.map((value) => row(value)).join("")}</Table></Worksheet>`; return `<?xml version="1.0"?><Workbook xmlns="urn:schemas-microsoft-com:office:spreadsheet" xmlns:ss="urn:schemas-microsoft-com:office:spreadsheet"><Styles><Style ss:ID="Cell"><Alignment ss:Vertical="Top" ss:WrapText="1"/><Font ss:FontName="Segoe UI" ss:Size="10"/><Borders><Border ss:Position="Bottom" ss:LineStyle="Continuous" ss:Weight="1" ss:Color="#D7E1EC"/></Borders></Style><Style ss:ID="Brand"><Font ss:FontName="Segoe UI" ss:Bold="1" ss:Color="#FFFFFF" ss:Size="12"/><Interior ss:Color="#0C1525" ss:Pattern="Solid"/></Style><Style ss:ID="Title"><Font ss:FontName="Segoe UI" ss:Bold="1" ss:Size="16" ss:Color="#10243C"/><Interior ss:Color="#E7F5FA" ss:Pattern="Solid"/></Style><Style ss:ID="Heading"><Font ss:FontName="Segoe UI" ss:Bold="1" ss:Color="#FFFFFF"/><Interior ss:Color="#187E9D" ss:Pattern="Solid"/></Style><Style ss:ID="Subtle"><Font ss:FontName="Segoe UI" ss:Italic="1" ss:Color="#52657A"/></Style></Styles>${sheet("Executive report", "Executive verdict", [["Verdict", data.verdict.join(" ")], ["Risk assessment", `${data.riskScore} / 100 (${capitalize(data.riskLevel)})`], ["Trust assessment", `${data.trustScore} / 100 (${capitalize(data.trustLevel)})`], ["Confidence", `${data.confidence}%`], ["Priority", data.priority], ["Recommendation", data.recommendation], ["Reason", data.recommendationReason], ["Summary", data.summary]])}${sheet("Evidence", "Evidence collected", data.evidence.map((value, index) => [`Evidence ${index + 1}`, value]))}${sheet("Technical details", "File and signature details", [...data.details, ...data.signature])}${sheet("Risk factors", "Scored risk factors", data.riskFactors.length ? data.riskFactors.map((value, index) => [`Factor ${index + 1}`, value]) : [["Finding", "No scored risk factors were retained."]])}</Workbook>`; }
function pdfDocument(data: ExportReport): string { const pages = [pdfPage(data, "VERDICT AND ASSESSMENT", [["Verdict", data.verdict], ["Recommendation", [data.recommendation, data.recommendationReason]], ["Executive summary", [data.summary]]], 1, true), pdfPage(data, "EVIDENCE AND FILE DETAILS", [["Evidence collected", data.evidence], ["File details", data.details.map(([label, value]) => `${label}: ${value}`)]], 2), pdfPage(data, "RISK AND SIGNATURE REVIEW", [["Risk factors", data.riskFactors.length ? data.riskFactors : ["No scored risk factors were retained."]], ["Digital signature", data.signature.map(([label, value]) => `${label}: ${value}`)], ["Scope", ["This report contains static local evidence. It does not execute the file, establish intent, or replace dynamic sandbox analysis."]]], 3)]; return createPdf(pages); }
function pdfPage(data: ExportReport, title: string, sections: Array<[string, string[]]>, pageNumber: number, includeMetrics = false): string { let y = 718; let body = "q\n0.047 0.082 0.145 rg\n0 748 612 94 re f\n0.094 0.494 0.616 rg\n0 744 612 4 re f\nQ\n"; body += pdfText("F2", 13, 48, 807, "viAI SECURITY"); body += pdfText("F1", 8, 48, 788, "LOCAL EVIDENCE REPORT"); body += pdfText("F1", 9, 48, 728, title); body += pdfText("F1", 8, 542, 728, `Page ${pageNumber} of 3`); if (includeMetrics) { const metrics = [["RISK", `${data.riskScore} / 100`, capitalize(data.riskLevel)], ["TRUST", `${data.trustScore} / 100`, capitalize(data.trustLevel)], ["CONFIDENCE", `${data.confidence}%`, `${data.priority} priority`]]; metrics.forEach(([label, value, detail], index) => { const x = 48 + index * 172; body += `q\n0.91 0.96 0.98 rg\n${x} 640 156 58 re f\nQ\n${pdfText("F1", 7, x + 10, 684, label)}${pdfText("F2", 14, x + 10, 664, value)}${pdfText("F1", 8, x + 10, 651, detail)}`; }); y = 620; }
  for (const [heading, values] of sections) { body += `q\n0.91 0.96 0.98 rg\n48 ${y - 16} 516 18 re f\nQ\n${pdfText("F2", 9, 56, y - 4, heading.toUpperCase())}`; y -= 31; for (const value of values.flatMap((line) => wrapPdf(line, 80))) { body += pdfText("F1", 9, 58, y, value); y -= 13; } y -= 12; }
  body += pdfText("F1", 7, 48, 34, "Generated locally by viAI. Static evidence supports investigation; it does not determine intent."); return body; }
function createPdf(pages: string[]): string { const objects: string[] = ["<< /Type /Catalog /Pages 2 0 R >>", `<< /Type /Pages /Kids [${pages.map((_, index) => `${3 + index * 2} 0 R`).join(" ")}] /Count ${pages.length} >>`]; pages.forEach((content, index) => { const page = 3 + index * 2; const stream = page + 1; objects.push(`<< /Type /Page /Parent 2 0 R /MediaBox [0 0 612 842] /Resources << /Font << /F1 << /Type /Font /Subtype /Type1 /BaseFont /Helvetica >> /F2 << /Type /Font /Subtype /Type1 /BaseFont /Helvetica-Bold >> >> >> /Contents ${stream} 0 R >>`, `<< /Length ${content.length} >>\nstream\n${content}\nendstream`); }); let output = "%PDF-1.4\n"; const offsets = [0]; objects.forEach((object, index) => { offsets.push(output.length); output += `${index + 1} 0 obj\n${object}\nendobj\n`; }); const xref = output.length; output += `xref\n0 ${objects.length + 1}\n0000000000 65535 f \n${offsets.slice(1).map((offset) => `${String(offset).padStart(10, "0")} 00000 n \n`).join("")}trailer\n<< /Size ${objects.length + 1} /Root 1 0 R >>\nstartxref\n${xref}\n%%EOF`; return output; }
function pdfText(font: "F1" | "F2", size: number, x: number, y: number, value: string): string { return `BT\n/${font} ${size} Tf\n0.07 0.14 0.23 rg\n${x} ${y} Td\n(${escapePdf(value)}) Tj\nET\n`; }
function wrapPdf(value: string, width: number): string[] { const words = value.replace(/[^\x20-\x7E]/g, "?").split(/\s+/); const lines: string[] = []; let line = ""; words.forEach((word) => { if (`${line} ${word}`.trim().length > width && line) { lines.push(line); line = word; } else line = `${line} ${word}`.trim(); }); if (line) lines.push(line); return lines.length ? lines : ["Not recorded"]; }
function escapePdf(value: string): string { return value.replace(/[^\x20-\x7E]/g, "?").replace(/[\\()]/g, "\\$&"); }
function escapeXml(value: string): string { return value.replace(/[&<>"']/g, (character) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", "\"": "&quot;", "'": "&apos;" })[character] ?? character); }
function escapeHtml(value: string): string { return value.replace(/[&<>"']/g, (character) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", "\"": "&quot;", "'": "&#39;" })[character] ?? character); }
function risk(score: number | undefined): "low" | "medium" | "high" { return (score ?? 0) <= 25 ? "low" : (score ?? 0) <= 60 ? "medium" : "high"; }
function capitalize(value: string): string { return value ? `${value[0].toUpperCase()}${value.slice(1)}` : "Not recorded"; }
function formatDate(value: string): string { const date = new Date(value); return Number.isNaN(date.valueOf()) ? "Not recorded" : date.toLocaleString(); }
function formatDuration(milliseconds: number): string { return `${(milliseconds / 1_000).toFixed(2)} seconds`; }
function formatBytes(value: number): string { return `${(value / 1024).toFixed(1)} KB`; }

const reportStyles = `:root{font-family:"Segoe UI Variable","Segoe UI",sans-serif;color:#14243b;background:#eef4f7}*{box-sizing:border-box}body{margin:0}main{max-width:980px;margin:28px auto;background:#fff;box-shadow:0 18px 50px #17335a1c}header{min-height:76px;padding:16px 38px;background:#0c1525;color:#fff;display:flex;justify-content:space-between;align-items:center;border-bottom:4px solid #187e9d}.brand{display:flex;gap:11px;align-items:center;font-size:20px;letter-spacing:0}.brand strong{font-weight:750}.brand small{display:block;font-size:8px;letter-spacing:2px;color:#82d0e4;margin-top:2px}.mark{width:34px;height:34px;display:grid;place-items:center;background:#187e9d;color:#fff;font-weight:800;clip-path:polygon(50% 0,92% 14%,86% 72%,50% 100%,14% 72%,8% 14%)}.meta{font-size:10px;letter-spacing:1px;color:#b5c5d7;text-align:right}.meta time{display:block;margin-top:5px;letter-spacing:0}.hero{padding:38px;border-bottom:1px solid #dbe6ed}.hero p,.label{margin:0 0 9px;color:#187e9d;font-size:11px;font-weight:750;letter-spacing:1.4px}.hero h1{margin:0 0 9px;font-size:29px;overflow-wrap:anywhere}.hero span{color:#597087;overflow-wrap:anywhere}.verdict,main>section:not(.hero),.grid section{padding:28px 38px;border-bottom:1px solid #dbe6ed}.verdict{background:#f6fbfc}.verdict h2,main>section h2{margin:0 0 12px;font-size:20px}.verdict p:not(.label),main>section>p{margin:8px 0;line-height:1.55}.metrics{display:grid;grid-template-columns:repeat(3,1fr);gap:1px;background:#dbe6ed;border-bottom:1px solid #dbe6ed}.metrics article{padding:23px 38px;background:#fff}.metrics span{display:block;color:#597087;font-size:12px}.metrics strong{display:block;margin:6px 0;font-size:28px;color:#10243c}.metrics small{font-size:13px;color:#597087}.metrics b{font-size:12px;font-weight:650;color:#187e9d}.metrics b.medium{color:#9a6500}.metrics b.high{color:#ad3346}.summary{font-size:16px}.grid{display:grid;grid-template-columns:1fr 1fr}.grid section{min-width:0}ul{margin:0;padding-left:19px}li{padding:5px 0;line-height:1.45}table{width:100%;border-collapse:collapse;font-size:13px}th,td{padding:9px 0;text-align:left;vertical-align:top;border-bottom:1px solid #e3ebf0;overflow-wrap:anywhere}th{width:42%;color:#597087;font-weight:600}footer{padding:24px 38px;background:#0c1525;color:#b5c5d7;font-size:11px;display:flex;justify-content:space-between;gap:20px}footer strong{color:#fff;white-space:nowrap}@media(max-width:680px){main{margin:0}.meta{display:none}.hero,.verdict,main>section:not(.hero),.grid section{padding:24px}.metrics,.grid{grid-template-columns:1fr}.metrics article{padding:20px 24px}footer{padding:20px 24px;display:block}footer span{display:block;margin-top:8px}}@media print{body{background:#fff}main{margin:0;max-width:none;box-shadow:none}.grid section,.metrics article,.verdict{break-inside:avoid}`;